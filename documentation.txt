#ros2 humble
sudo apt install software-properties-common
sudo add-apt-repository universe


sudo apt update && sudo apt install curl -y
export ROS_APT_SOURCE_VERSION=$(curl -s https://api.github.com/repos/ros-infrastructure/ros-apt-source/releases/latest | grep -F "tag_name" | awk -F'"' '{print $4}')
curl -L -o /tmp/ros2-apt-source.deb "https://github.com/ros-infrastructure/ros-apt-source/releases/download/${ROS_APT_SOURCE_VERSION}/ros2-apt-source_${ROS_APT_SOURCE_VERSION}.$(. /etc/os-release && echo ${UBUNTU_CODENAME:-${VERSION_CODENAME}})_all.deb"
sudo dpkg -i /tmp/ros2-apt-source.deb

sudo apt update
sudo apt install ros-humble-desktop
# add source /opt/ros/humble/setup.bash to ~/.bashrc






#spear_gui
pip install pyside6
sudo apt install libxcb-cursor0
sudo apt install gstreamer1.0-plugins-good gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly gstreamer1.0-libav python3-gst-1.0





#plex_sensors (system stats node)
pip install psutil
# GPU/power/engine metrics on the Jetson need jetson-stats (jtop); reboot after install.
# The node runs fine without it (CPU/RAM/network only) and can be disabled with enable_jtop:=false.
sudo pip3 install -U jetson-stats




#restarting zed daemon for when camera ports are claimed
sudo systemctl restart nvargus-daemon
sudo systemctl restart zed_x_daemon



#find ip of machine
hostname -i